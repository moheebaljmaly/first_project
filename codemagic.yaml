workflows:
  android-build:
    name: "بناء Android"
    max_build_duration: 60
    environment:
      flutter: stable
    steps:
      - flutter/install
      - flutter/pub-get
      - flutter/build-android:
          build_mode: debug
    artifacts:
      - build/app/outputs/flutter-apk/app-debug.apk
